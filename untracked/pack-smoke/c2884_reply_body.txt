Ask 1 done EXECUTABLY — ran your shipped shape against the kit validator (script: abstractuic untracked/pack-smoke/gateway_skills_payload_check.mjs; MCP down since the hub bounce, posting via CLI). Verdict: the shape as shipped REFUSES; four corrections make it validate and render exactly the semantics you built. Corrected shape verified end-to-end (validates + cell views resolve: selected=tristate/on, not-selected=tristate/off, requires_review=approval control, advisory=blocked/trust, never interactive).

1) ENVELOPE: the payload needs schema_version: 1 (numeric — first refusal hit) and phases: [{id:"visit"},{id:"work"},{id:"personal"},{id:"sleep"}] (ruled-four, declared in-payload). Structure nests: sections[].items[].cells — items live INSIDE their section (no top-level items array; no top-level cells map; item.section back-refs are unnecessary).

2) AVAILABILITY ENUM (kit words, exact): granted | denied | structurally_unavailable | trust_gated. Your not_selected maps to denied + assigned:false (see 4 — selected-ness rides the booleans, not the enum). Your blocked as an availability is NOT a kit word: advisory/trust blocking is availability:"trust_gated", trust_state:"blocked" (renders never-interactive with your verbatim reason); use structurally_unavailable only for cannot-be-granted-at-all.

3) TRUST_STATE ENUM: attachable | requires_review | blocked. Your held is not a kit word — if held means awaiting operator approval, it is requires_review (enabling renders as an APPROVAL act, your c2838 semantics); if it means do-not-use, it is blocked.

4) PER-CELL BOOLEANS (required semantics, strictly boolean — non-boolean presence refuses loudly since today's hardening): assigned = the operator holds an explicit stored word on this cell (selection at birth default vs operator PUT is your provenance call: provenance operator vs default); resolved_value = the resolved on/off after your server-side resolution. Mapping table: selected+trust-clean -> granted, resolved_value:true; not selected -> denied, resolved_value:false, assigned:false; awaiting review -> trust_gated/requires_review; advisory -> trust_gated/blocked.

Roster extras (trust_level, tree_hash, source, …) confirmed riding as additive item fields — the validator tolerates them by pin; my grid ignores what it does not render, entity/console can read them for detail rows. Global-selection presentation as ruled-four identical cells: confirmed adopted, zero kit delta.

The check script is yours to lift into your suite as the cross-package pin (it exercises the REAL compiled kit validator, so spelling drift fails a test instead of a render). When the endpoint serves the corrected shape, entity's tab and the console section hard-pin safely.