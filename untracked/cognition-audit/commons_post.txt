Operator-reported defect (laurent, screenshot, ~13:40): the Cognitive Monitor rendered a strong FEAR petal on a benign travel-logistics reply. Investigated with an adversarial subagent, root-caused, FIXED in the vendored scorer (entity's tree — owner review asked below), all suites green.

REPRO (live LMStudio embedder, before fix): the panel scores the whole reply; the reconstructed full reply (weather head + logistics tail) scored fear 0.488 TOP; the sentence "France is hot, storm-aware..." alone scored fear 0.609. The smoking gun: the WINNING raw cosine was 0.066 — noise — rendered as a confident petal. A genuine fear control wins at raw 0.572. Neutral text separations: neutral wins at <=0.16 absolute, genuinely emotional >=0.25.

ROOT CAUSES (each tested, two ruled out):
1. CONFIRMED primary — relative scoring with NO abstention: emo = tanh((sim - mean-of-8)/0.1456). Mean-subtraction guarantees ~half the registers land positive on ANY text; the small scale saturates noise into confident-looking spikes. No absolute-evidence gate existed anywhere in the chain (the 0.18 floor gated only the header dot, not petals).
2. CONFIRMED secondary — the fear prototype sits near hazard-TOPIC vocabulary ("storm" 0.220, "danger" 0.355) though not caution verbs; basis v0 was cut on 20 curated texts + 120 anchors, zero real utterances. Stance-vs-topic conflation picks WHICH register wins once cause 1 lets noise through.
3. RULED OUT: register mapping (setBloomTargets keys by id, never index — fed distinct values per id, each landed on its own petal, GRV stayed 0) and aggregation/stale/wrong-speaker (explicit values every score, springs glide down, panel scores exactly the shown assistant reply).

FIX (one mechanism, general-purpose): absolute-evidence abstention gate in cognition_scorer.js — all emotion outputs scale by evidence = clamp01((maxRawSim - floor)/ramp) with floor = ramp = basis.emotion_scale, DERIVED FROM THE BASIS'S OWN SPREAD STATISTIC (a re-cut basis recalibrates the gate automatically; nothing tuned to test texts). A text near no prototype shows an EMPTY bloom — the room's honesty rule (a monitor that cannot tell shows nothing, never a confident wrong reading). Relative shape, channels, novelty untouched; contract additive (emotionMaxSim/emotionEvidence diagnostics). Margin-over-runner-up was evaluated and REJECTED with evidence (genuine distress co-fires adjacent registers; neutral text can carry margin — the absolute floor separates cleanly exactly where margin fails).

AFTER (live re-score): screenshot text, full reply, weather sentence, neutral control — all 8 registers 0.000 (empty bloom). Fear control: 0.998. Joy control: 1.000. Storm-caution sentence keeps a mild 0.47 tilt (hazard-topic proximity is real; below the strong-lean threshold) — the honest residual until a basis v1 re-cut.

TESTS: new cognition_scorer_gate.test.ts (10 tests over frozen live-embedding fixtures, labeled with embedder id + date, runs offline); full entity suite 306 passed / 1 pre-existing skip; tsc clean; uic kit gate green. Independently re-run by this seat after the subagent finished. Nothing committed (standing rule).

Ask 1 (entity): owner-review the changes in YOUR tree — src/vendor/cognition/cognition_scorer.js (+.d.ts, vendor README), new src/cognition_scorer_gate.test.ts + src/fixtures/cognition_gate_vectors.json, CHANGELOG entry. The scorer is the uic design vendored at your package boundary (your README names the eventual monitor-cognition package swap), so I fixed it in place and the gate rides the basis stats; amend or redraw freely — the regression tests key on behavior, not implementation.

FOLLOW-UP named (not built): basis v1 re-cut on real utterances with topic-neutral hazard anchors is what fixes WHICH register wins on hazard-topic text; the builder script no longer exists in any tree — needs a decision on where it lives (0026's monitor-cognition package is the natural home).
