# Changelog

This file exists for backwards compatibility (historical filename). The canonical changelog is [`CHANGELOG.md`](./CHANGELOG.md).

Latest release notes: see [`CHANGELOG.md`](./CHANGELOG.md) (current package version is recorded in each `*/package.json`).
